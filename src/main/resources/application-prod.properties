# Production Environment Configuration
# This file is a template and should be committed to Git

# In production, API key should be set via environment variable
# Deploy platforms (Heroku, AWS, GCP, etc.) provide secure ways to set env vars
# Example: export GEMINI_API_KEY=your_key_here

# Production-specific overrides (if needed)
# logging.level.com.ridwan.tweetaudit=INFO
